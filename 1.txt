SELECT t1.person_id, t2.pass_type
FROM your_table_name t1
INNER JOIN (
  SELECT person_id
  FROM your_table_name
  WHERE status = 'VALID'
  GROUP BY person_id
  ORDER BY COUNT(DISTINCT pass_type) DESC
  FETCH FIRST 10 ROWS ONLY
) t2 ON t1.person_id = t2.person_id
WHERE t1.status = 'VALID';
